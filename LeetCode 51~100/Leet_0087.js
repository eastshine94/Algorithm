/**
 * @param {string} s1
 * @param {string} s2
 * @return {boolean}
 */
let answer = false;
var isScramble = function(s1, s2) {
    scramble(s1,s2);
    return answer;
};

const scramble = (s1, s2) => {
    
}